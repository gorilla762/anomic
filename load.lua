--// load modules into the game when executed

if game.PlaceId ~= 4581966615 then
	warn('Halted execution, game is not compatible for the script.')
elseif rconsole then
	
elseif not rconsole then
	print('Loading modules.')
	
end
